import { CodeDirectiveDirective } from './code-directive.directive';

describe('CodeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
