import { PandirectiveDirective } from './pandirective.directive';

describe('PandirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PandirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
